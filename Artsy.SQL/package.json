{
  "name": "artsy.sql",
  "version": "1.0.0",
  "description": "PostgreSQL schema deployment generator for the Artsy framework.",
  "private": true,
  "scripts": {
    "update-deploy": "gulp update-deploy",
    "watch": "gulp watch"
  },
  "devDependencies": {
    "gulp": "^5.0.0"
  }
}
